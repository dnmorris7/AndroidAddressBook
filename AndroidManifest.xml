<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codecrunchcorner.addressbookapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.codecrunchcorner.addressbookapp.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        //BUG RESOLVED: Needed to "declare" new activities in the manifest using this alternative methods to forming an onClick listener
        <activity
            android:name="com.codecrunchcorner.addressbookapp.NewContact"
            android:label="" >
        </activity>
        <activity
            android:name="com.codecrunchcorner.addressbookapp.EditContact"
            android:label="" >
        </activity>
        
    </application>

</manifest>
